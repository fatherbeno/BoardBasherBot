/**
 * Required information to facilitate each separate file in the file system.
 * @author Benjamin Gulliver (fatherbeno)
 */
export interface IFileInfo {
    fileFolder: string,
    fileName: string,
    fileBase: string
}